{
  "name": "sara",
  "version": "1.0.0",
  "description": "this is a test fluter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-compile": "sass --watch sass:css -q",
    "devserver": "live-server --port=8082 -q --no-browser",
    "start": "npm-run-all --parallel devserver sass-compile"
  },
 
  "author": "olive",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^7.1.1"
  }
}
